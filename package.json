{
  "name": "lado-b2b-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Chạy đồng thời server (Express, port 4000) + client (Vite, port 5173) chỉ bằng 1 lệnh npm run dev ở thư mục gốc.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n SERVER,CLIENT -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}